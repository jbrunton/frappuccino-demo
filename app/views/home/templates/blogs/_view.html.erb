<div class="page-header">
    <h1>
        <span data-bind="text: title" />
        <small class="pull-right">
            <a class="nav-to" data-bind="attr: { href: edit_url_for( $data ) }">edit</a>
        </small>
    </h1>
</div>

<div class="row">
    <section class="span8">
        <p class="leader" data-bind="text: description" />
        <div data-bind="template: { name: 'template:blog_posts/list', data: blog_posts() }" />
    </section>
</div>